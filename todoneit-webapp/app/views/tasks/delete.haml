- @title = @task.description

#confirmation
  %form(action="/tasks/#{@task.id}/delete" method="post")
    %label(for="confirm") 
      %span Are you sure you want to delete this task?
    %input(type="checkbox" name="confirm" value="#{false}")
    %input(type="submit" value="Ok")
